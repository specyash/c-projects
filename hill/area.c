#include <stdio.h>

int main(){
    int length;
    int breadth;
    printf("Enter Length\n");
    scanf("%d", &length);

    printf("Enter Breadth\n");
    scanf("%d", &breadth);

    printf("The area of the rectange is %d\n", length*breadth);

    return 0;
}